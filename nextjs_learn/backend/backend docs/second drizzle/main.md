**Phase 1 — SQL Basics**
- SQL kya hai, MongoDB se kaise alag
- Tables, rows, columns, primary key
- Basic queries — SELECT, INSERT, UPDATE, DELETE — concept samjho

**Phase 2 — PostgreSQL + Neon Setup**
- PostgreSQL kya hai
- Neon pe account, database, connection string
- Atlas setup jaisi detail mein — har click explain karunga

**Phase 3 — Drizzle Basics**
- ORM kya hota hai
- Drizzle install + setup
- Schema banao — Todo table

**Phase 4 — Todo App — Drizzle Se**
- CRUD operations — MongoDB wale compare karte hue
- API Routes se connect
- Frontend same — sirf backend alag

***Each phase can have as many number of docs as they want as each doc will be not that big ... 
so we have two choice either put all in a single doc or divide them in multiple i want to divide them in multiple ..
