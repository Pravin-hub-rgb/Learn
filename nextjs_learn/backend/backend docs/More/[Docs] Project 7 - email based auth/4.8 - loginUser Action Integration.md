# Doc 4.8 — loginUser Action Integration

## Abhi Tak Kya Hua

- `createSession()` helper 100% ready hai
- Session model ban chuka hai
- Sab types bhi generate ho gaye hain
- ✅ **Aaj hum ab isko apne loginUser action mein integrate karenge**

## Aaj Ka Goal (4.8)

✅ `loginUser` action mein `createSession()` add karenge
✅ User ko successful login ke baad dashboard par redirect karenge
✅ Login flow finally poora complete karenge

---

## Step 1: Abhi Ka loginUser Action

✅ Ab tumhare paas `app/actions/auth.ts` mein yeh code hoga abhi:

```typescript
"use server";

import prisma from "@/lib/prisma";
import { compare } from "@/lib/password";
import { LoginSchema } from "@/lib/validations";
import z from "zod";

export async function loginUser(values: z.infer<typeof LoginSchema>) {
  const validatedFields = LoginSchema.safeParse(values);

  if (!validatedFields.success) {
    return { error: "Invalid fields" };
  }

  const { email, password } = validatedFields.data;

  const existingUser = await prisma.user.findUnique({
    where: { email }
  });

  if (!existingUser) {
    return { error: "User not found" };
  }

  if (!existingUser.isVerified) {
    return { error: "Please verify your email first" };
  }

  const passwordMatch = await compare(password, existingUser.password);

  if (!passwordMatch) {
    return { error: "Invalid credentials" };
  }

  // ✅ AB YAHAN PAR KUCH AUR KARNA HAI
  return { success: "Login successful" };
}
```

> ✅ Ab yaha tak humne sab checks kar liye hai.
> ✅ Agar yaha tak code aa gaya hai toh login 100% successful hai.
> ✅ Ab bas ab humein session create karna hai aur redirect karna hai.

---

## Step 2: createSession Import Karo

✅ Sab se pehle import add karo file ke top par:

```typescript
import { createSession } from "@/lib/session";
import { redirect } from "next/navigation";
```

> ⚠️ **VERY IMPORTANT:**
> Tumhe `next/navigation` se import karna hai, `next` ya kisi aur jagah se nahi.
> Agar tum galat import karoge toh woh Next.js Pages Router wala redirect hoga jo 2 parameters maangta hai.
> App Router wala redirect sirf ek parameter leta hai.
> ✅ Sahi hai: `import { redirect } from "next/navigation";`
> ❌ Galat hai: `import redirect from 'next/redirect'` ya kahi aur se

> 🧠 Kya kyun import kiya:
> - `createSession` → Woh function jo humne kal banaya tha session create karne ke liye
> - `redirect` → Next.js ka function hai jo user ko dusre page par bhej deta hai

---

## Step 3: Session Create Karo

✅ Ab password match hone ke baad yeh line add karo:

```typescript
if (!passwordMatch) {
  return { error: "Invalid credentials" };
}

// ✅ Step 4: Session create karo
await createSession(existingUser.id);
```

> 🧠 Kya ho raha hai yaha:
> - Hum user ke id ko `createSession` ko de rahe hai
> - Woh function database mein session create karega
> - Woh function browser mein cookie set karega
> - Sab kuch us function mein already hai humein bas call karna hai

---

## Step 4: User Ko Redirect Karo

✅ Uske baad yeh line add karo:

```typescript
await createSession(existingUser.id);

// ✅ Step 5: Dashboard par redirect karo
redirect("/dashboard");
```

> 🧠 Kya hoga ab:
> Jab user sahi email + password daalega:
> 1.  Sab checks pass honge
> 2.  Session create hoga database mein
> 3.  Cookie set hogi browser mein
> 4.  User automatically dashboard par chala jayega

---

## Step 5: Ab Poora loginUser Action Yahi Dikhta Hai

✅ Final code:
```typescript
"use server";

import prisma from "@/lib/prisma";
import { compare } from "@/lib/password";
import { LoginSchema } from "@/lib/validations";
import { createSession } from "@/lib/session";
import { redirect } from "next/navigation";
import z from "zod";

export async function loginUser(values: z.infer<typeof LoginSchema>) {
  const validatedFields = LoginSchema.safeParse(values);

  if (!validatedFields.success) {
    return { error: "Invalid fields" };
  }

  const { email, password } = validatedFields.data;

  const existingUser = await prisma.user.findUnique({
    where: { email }
  });

  if (!existingUser) {
    return { error: "User not found" };
  }

  if (!existingUser.isVerified) {
    return { error: "Please verify your email first" };
  }

  const passwordMatch = await compare(password, existingUser.password);

  if (!passwordMatch) {
    return { error: "Invalid credentials" };
  }

  await createSession(existingUser.id);

  redirect("/dashboard");
}
```

---

## Step 6: Ab Test Karo

✅ Ab aise test karo:
1.  Login page par jao
2.  Sahi email + password daalo
3.  Submit karo

✅ Expected result:
✅ Tum automatically dashboard par chala jaoge
✅ Browser mein `sessionId` cookie set hogi
✅ Database mein Session table mein ek nayi entry ban gayi hogi
✅ Prisma Studio se dekh sakte ho

---

## Summary

**Aaj humne kya kiya:**
1.  `createSession` function import kiya
2.  `redirect` function import kiya
3.  Password match hone ke baad session create kiya
4.  User ko dashboard par redirect kiya
5.  ✅ Login flow ab poora 100% complete ho gaya hai!

✅ Ab user successfully login kar sakta hai.

---

## Agla Step (4.9)

Phase 4.9 mein hum `getSession()` helper banayenge jisse hum har request par check kar sakte hai ki user login hai ya nahi.