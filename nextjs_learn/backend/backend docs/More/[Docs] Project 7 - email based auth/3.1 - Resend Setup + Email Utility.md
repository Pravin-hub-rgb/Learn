# Doc 3.1 — Resend Setup + Email Utility: `lib/email.ts`

## Abhi Tak Kya Hua

- **Phase 2 complete** — Registration system ban gaya!
- User register kar sakta hai, database mein save hota hai
- Password hash ho raha hai, validation ho raha hai
- **Next:** Ab email verification system banayenge — user ko verification email bhejenge

## Big Picture: Hum Kahan Hain?

```
┌─────────────────────────────────────────────────────────────────┐
│                    CURRENT STATE (Phase 2)                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  User fills form → registerUser() → Database mein save          │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  PROBLEM: Koi bhi fake email se register kar sakta hai! │   │
│  │                                                           │   │
│  │  Example:                                               │   │
│  │  - abc@xyz.com (valid format, but not a real email)     │   │
│  │  - asdf@ghjk.com (format sahi, email nahi hai)          │   │
│  │                                                           │   │
│  │  Exploit:                                               │   │
│  │  - Spam accounts create ho sakte hain                   │   │
│  │  - Fake users database pile up ho sakte hain            │   │
│  │  - Koi bhi kisi aur ke email se register kar sakta hai  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                   SOLUTION (Phase 3)                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  1. Register → Verification Token generate → Email bhejo        │
│  2. User email verify kare → isVerified = true                  │
│  3. Login → isVerified check → Session bane                     │
│  4. Dashboard (protected)                                       │
│                                                                   │
│  RESULT: Sirf verified emails login kar sakte hain!             │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

## Broader Goal

**Aaj kya karenge:**

- Resend account banayenge — email bhejne ke liye
- `resend` package install karenge
- `lib/email.ts` banayenge — `sendEmail()` helper
- Test email bhejenge — inbox mein verify karenge

**Problem jo hum solve karenge:**

```
User registered hai ✓
User ko verification email bhejna hai → Abhi koi email system nahi hai! ❌
```

## Why Email Verification? (Kyun Zaroori Hai?)

**Without verification (Current State):**
```typescript
// Koi bhi fake email se register kar sakta hai
// Server Action call hoti hai:
registerUser(formData);
// → User created! ❌
```

**With verification (Phase 3):**
```typescript
// Register hoga, lekin login nahi kar payega
// Server Action call hoti hai:
registerUser(formData);
// → Verification email bheja (jo kabhi verify nahi hoga)

loginUser(formData);
// → Error: "Please verify your email first" ✅
```

**Benefits:**
1. **Spam prevention** — Fake accounts kam honge
2. **Email ownership** — User actually owns the email
3. **Security** — Koi aur ke email se register nahi kar sakta
4. **Clean database** — Sirf verified users

## Email Service Options: Kyun Resend Chuna?

**Different email sender services:**

| Service | Pros | Cons | Pricing |
|---------|------|------|---------|
| **Resend** | Modern API, Developer-friendly, React email templates | Newer, fewer integrations | Free 100 emails/day |
| **SendGrid** | Established, Many integrations | Complex API, Overkill for small apps | Free 100 emails/day |
| **Nodemailer** | Free, Self-hosted | Need SMTP server, Maintenance | Free (self-hosted) |
| **AWS SES** | Cheap at scale, Reliable | Complex setup, AWS account needed | $0.10 per 1000 emails |
| **Postmark** | Fast delivery, Great deliverability | Expensive, Limited free tier | Free 100 emails/month |

**Why we chose Resend:**

1. **Developer Experience:**
   ```typescript
   // Resend — Simple and clean
   await resend.emails.send({ from, to, subject, html });

   // SendGrid — More verbose
   const msg = { to, from, subject, html };
   await sg.send(msg);
   ```

2. **Modern Stack:**
   - Built for React/Next.js developers
   - React-based email templates
   - TypeScript support out of the box

3. **Free Tier:**
   - 100 emails/day (3000/month) — enough for development
   - No credit card required

4. **Easy Setup:**
   - No SMTP configuration
   - No DNS records (for testing)
   - API key = ready to go

5. **Next.js Integration:**
   - Made by Vercel team
   - Works seamlessly with Server Actions
   - Edge-compatible

**Hum Resend use kar rahe hain kyunki:**
- Simple API — kam code, zyada kaam
- Free tier sufficient hai development ke liye
- Next.js/React developers ke liye best experience
- Production-ready when needed

## Unverified Users ka Cleanup Strategy

**Problem:** Agar user verify nahi karta, toh woh database mein pile up hote rahenge!

**Solutions:**

### Option A: Auto-cleanup via Token Expiry (Implemented)
- `VerificationToken` model mein `expiresAt` field hai (24 hours)
- Expired tokens automatically kaam nahi karenge
- User ko dobara register karna padega
- **Pros:** Simple, automatic
- **Cons:** Unverified users abhi bhi database mein rahenge

### Option B: Manual Cleanup (Using in this project)
- Admin panel se unverified users delete kar sakte hain
- Ya manually query run kar sakte hain:

```sql
-- Delete unverified users older than 7 days
DELETE FROM "User" 
WHERE "isVerified" = false 
AND "createdAt" < NOW() - INTERVAL '7 days';
```

**Pros:** Full control
**Cons:** Manual effort required

### Option C: Periodic Cleanup (Best for production)
- Background job jo roz unverified users ko clean kare

```typescript
// lib/cleanup.ts — Syntax example

import prisma from "./prisma";

export async function cleanupUnverifiedUsers() {
  const result = await prisma.user.deleteMany({
    where: {
      isVerified: false,
      createdAt: {
        lt: new Date(Date.now() - 7 * 24 * 60 * 60 * 1000), // 7 days ago
      },
    },
  });

  console.log(`Cleaned up ${result.count} unverified users`);
  return result.count;
}
```

**Cron job example (using `node-cron`):**
```typescript
// app/cleanup/route.ts

import { cleanupUnverifiedUsers } from "@/lib/cleanup";
import cron from "node-cron";

// Roz raat 2 baje cleanup
cron.schedule("0 2 * * *", async () => {
  await cleanupUnverifiedUsers();
});
```

**Pros:** Automatic, scalable
**Cons:** Extra package, complexity

**Hum is project mein Option B use karenge** — simple manual cleanup. Production ke liye Option C implement kar sakte hain.

---

## Step-by-Step Implementation

### Step 1: Problem Samjho — "Email Kaise Bhejein?"

Soch rahe ho: *"Mera registration system toh ban gaya. Ab user ko verification email kaise bhejun?"*

**Requirements:**
1. Email bhejne ke liye ek service chahiye
2. Node.js se email bhej sakte hain
3. Simple API — `sendEmail()` function
4. Test karne mein aasaan ho

**Solution:** Resend — modern email API for developers

### Step 2: Resend Account Banao

1. [Resend](https://resend.com) pe jao
2. Sign up karo — GitHub/Google se easily ho jayega
3. Dashboard pe jao
4. API key copy karo — `.env` mein daalne ke liye

**Dashboard mein:**
- `API Keys` section
- `re_...` format ki key hogi
- Copy karlo

### Step 3: `.env` Mein API Key Add Karo

```env
# .env

DATABASE_URL="postgresql://..."
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxx"
```

**Note:** API key ko commit mat karna — `.gitignore` mein `.env` already hoga.

### Step 4: Resend Package Install Karo

```bash
npm install resend
```

**Kya install hua:**
- `resend` — Resend ka official SDK
- Email bhejne ke liye simple API

### Step 5: `lib/email.ts` Banao — Resend Official Template Se Start

**Developer ka thought process:**

Soch rahe ho: *"Mujhe email bhejni hai. Resend use karunga. Resend ki documentation mein kya diya hai? Chalo dekhte hain."*

#### Resend Official Node.js Template

Resend ki website par yeh template diya hai:

```javascript
import { Resend } from 'resend';

const resend = new Resend('re_123456789');

resend.emails.send({
  from: 'onboarding@resend.dev',
  to: 'user@example.com',
  subject: 'Hello World',
  html: '<p>Congrats on sending your first email!</p>'
});
```

**Template samjho — yeh kya kar raha hai?**

1. **`import { Resend } from 'resend';`**
   - Resend package se `Resend` class import kar rahe hain
   - Yeh class email bhejne ke liye use hoti hai

2. **`const resend = new Resend('re_123456789');`**
   - `Resend` class ka naya instance bana rahe hain
   - Constructor mein API key de rahe hain
   - `'re_123456789'` — yeh example API key hai

3. **`resend.emails.send({...});`**
   - `resend` instance ka `emails.send()` method call kar rahe hain
   - `from` — sender ka email address
   - `to` — receiver ka email address
   - `subject` — email ka subject
   - `html` — email ka HTML content

**Humara Goal:**
- Is template ko humare project mein adapt karna hai
- API key `.env` se leni hai (hardcode nahi karni)
- Reusable function banana hai — `sendEmail()`
- Error handling add karni hai
- TypeScript types add karne hain

#### Step 5.1: Template Ko Humare Project Mein Adapt Karo

*"Sabse pehle, template ko humare project mein copy karte hain. Lekin API key hardcode nahi karenge — `.env` se lenge."*

```typescript
// lib/email.ts

import { Resend } from "resend";

// API key .env se lo — hardcode mat karo!
const resend = new Resend(process.env.RESEND_API_KEY);

// Template se direct copy — test ke liye
resend.emails.send({
  from: "onboarding@resend.dev",
  to: "prr3000@tutanota.com", // Apna email daalo
  subject: "Hello World",
  html: "<p>Congrats on sending your first email!</p>",
});
```

**Test:** Server restart karo aur dekho console mein koi error toh nahi hai.

#### Step 5.2: `sendEmail` Function Banao

*"Ab template ko reusable function mein convert karte hain. Function ko `sendEmail` bolenge. Isme `to`, `subject`, aur `html` parameters lenge."*

```typescript
// lib/email.ts

import { Resend } from "resend";

const resend = new Resend(process.env.RESEND_API_KEY);

// Reusable function — parameters ke saath
export async function sendEmail({ to, subject, html }: { to: string; subject: string; html: string }) {
  // Template se copy — parameters ke saath
  await resend.emails.send({
    from: "onboarding@resend.dev",
    to,
    subject,
    html,
  });
}
```

**Explanation:**
- `export async function` — function ko export karte hain taaki doosre files use kar sakein
- `async` — kyunki `send()` asynchronous hai
- `{ to, subject, html }` — object destructuring, teen parameters
- `{ to: string; subject: string; html: string }` — TypeScript types
- `await` — kyunki `send()` promise return karta hai

#### Step 5.3: Return Value Handle Karo

*"Resend ka `send()` method ek object return karta hai jisme `data` aur `error` hota hai. Dono ko handle karna padega."*

```typescript
// lib/email.ts

import { Resend } from "resend";

const resend = new Resend(process.env.RESEND_API_KEY);

export async function sendEmail({ to, subject, html }: { to: string; subject: string; html: string }) {
  // Resend ka response handle karo
  const { data, error } = await resend.emails.send({
    from: "onboarding@resend.dev",
    to,
    subject,
    html,
  });

  // Response check karo
  if (error) {
    console.error("Failed to send email:", error);
    throw error;
  }

  // Success — return karo
  return { success: true, id: data?.id };
}
```

**Explanation:**
- `{ data, error }` — Resend do cheezein return karta hai
- `if (error)` — check karo ki error toh nahi
- `console.error` — error ko console mein dikhao
- `throw error` — error ko upar phenko taaki caller handle kar sake
- `return { success: true, id: data?.id }` — caller ko batane ke liye

#### Step 5.4: Try-Catch Error Handling Add Karo

*"Ab unexpected errors ko handle karne ke liye `try-catch` add karte hain."*

```typescript
// lib/email.ts

import { Resend } from "resend";

const resend = new Resend(process.env.RESEND_API_KEY);

export async function sendEmail({ to, subject, html }: { to: string; subject: string; html: string }) {
  try {
    const { data, error } = await resend.emails.send({
      from: "onboarding@resend.dev",
      to,
      subject,
      html,
    });

    if (error) {
      console.error("Failed to send email:", error);
      throw error;
    }

    return { success: true, id: data?.id };
  } catch (error) {
    console.error("Error sending email:", error);
    throw error;
  }
}
```

**Explanation:**
- `try` block — main code jo error de sakta hai
- `catch` block — unexpected errors handle karta hai
- Dono jagah `throw error` — caller ko error pata chale

#### Final Code:

```typescript
// lib/email.ts

import { Resend } from "resend";

// Resend client initialize karo
const resend = new Resend(process.env.RESEND_API_KEY);

/**
 * Email bhejne ka helper function
 * @param to - kisko bhejna hai
 * @param subject - email ka subject
 * @param html - email ka HTML content
 */
export async function sendEmail({
  to,
  subject,
  html,
}: {
  to: string;
  subject: string;
  html: string;
}) {
  try {
    const { data, error } = await resend.emails.send({
      from: "onboarding@resend.dev", // Resend ka default domain
      to,
      subject,
      html,
    });

    if (error) {
      console.error("Failed to send email:", error);
      throw error;
    }

    console.log("Email sent successfully:", data?.id);
    return { success: true, id: data?.id };
  } catch (error) {
    console.error("Error sending email:", error);
    throw error;
  }
}
```

**Key points:**
- `Resend` class ko API key se initialize karte hain
- `sendEmail()` function simple wrapper hai
- `from` address — Resend ka default domain (`onboarding@resend.dev`)
- Error handling — agar email nahi gaya toh error throw karega

### Step 6: Test Email Bhejo

Ab test karenge ki email system kaam kar raha hai.

Nayi file banao: `app/test-email/page.tsx` (temporary — sirf testing ke liye)

```tsx
// app/test-email/page.tsx

import { sendEmail } from "@/lib/email";

export default async function TestEmailPage() {
  try {
    const result = await sendEmail({
      to: "your-email@example.com", // Apna email daalo
      subject: "Test Email from Resend",
      html: "<h1>Hello!</h1><p>Yeh test email hai.</p>",
    });

    return (
      <div>
        <h1>Email Sent!</h1>
        <pre>{JSON.stringify(result, null, 2)}</pre>
      </div>
    );
  } catch (error) {
    return (
      <div>
        <h1>Email Failed!</h1>
        <pre>{JSON.stringify(error, null, 2)}</pre>
      </div>
    );
  }
}
```

**Test kaise karein:**
1. `your-email@example.com` ko apna actual email se replace karo
2. Browser mein jao: `http://localhost:3000/test-email`
3. Page load hoga — email bhejega
4. Apne inbox mein check karo

**Expected:**
- Email aaya: "Test Email from Resend"
- Subject: "Test Email from Resend"
- Content: "Hello! Yeh test email hai."

### Step 7: Default Domain Change Karo (Optional)

Resend ke free plan mein `onboarding@resend.dev` domain use hota hai. Agar apna custom domain use karna hai:

1. Resend dashboard mein jao
2. Domains section mein jao
3. "Add Domain" click karo
4. Domain verify karo (DNS records add karne honge)
5. `lib/email.ts` mein `from` change karo:

```typescript
from: "yourname@yourdomain.com", // Apna custom domain
```

---

## Complete Code

### `lib/email.ts`

```typescript
// lib/email.ts

import { Resend } from "resend";

// Resend client initialize karo
const resend = new Resend(process.env.RESEND_API_KEY);

/**
 * Email bhejne ka helper function
 * @param to - kisko bhejna hai
 * @param subject - email ka subject
 * @param html - email ka HTML content
 */
export async function sendEmail({
  to,
  subject,
  html,
}: {
  to: string;
  subject: string;
  html: string;
}) {
  try {
    const { data, error } = await resend.emails.send({
      from: "onboarding@resend.dev",
      to,
      subject,
      html,
    });

    if (error) {
      console.error("Failed to send email:", error);
      throw error;
    }

    console.log("Email sent successfully:", data?.id);
    return { success: true, id: data?.id };
  } catch (error) {
    console.error("Error sending email:", error);
    throw error;
  }
}
```

### `app/test-email/page.tsx` (Temporary — Testing ke liye)

```tsx
// app/test-email/page.tsx

import { sendEmail } from "@/lib/email";

export default async function TestEmailPage() {
  try {
    const result = await sendEmail({
      to: "your-email@example.com", // Apna email daalo
      subject: "Test Email from Resend",
      html: "<h1>Hello!</h1><p>Yeh test email hai.</p>",
    });

    return (
      <div>
        <h1>Email Sent!</h1>
        <pre>{JSON.stringify(result, null, 2)}</pre>
      </div>
    );
  } catch (error) {
    return (
      <div>
        <h1>Email Failed!</h1>
        <pre>{JSON.stringify(error, null, 2)}</pre>
      </div>
    );
  }
}
```

---

## Testing Checklist

### ✅ Resend Setup
- [ ] Resend account bana
- [ ] API key copy ki
- [ ] `.env` mein `RESEND_API_KEY` add ki

### ✅ Code Setup
- [ ] `npm install resend` kiya
- [ ] `lib/email.ts` banaya
- [ ] `sendEmail()` function export kiya

### ✅ Test Email
- [ ] `app/test-email/page.tsx` banaya
- [ ] Apna email address daala
- [ ] `http://localhost:3000/test-email` pe gaya
- [ ] Email inbox mein aaya
- [ ] Email open kiya — content sahi tha

---

## Common Issues & Solutions

### Issue 1: Email nahi aaya

**Check:**
- `.env` mein `RESEND_API_KEY` sahi hai?
- API key `re_` se start hoti hai?
- Internet connection chal raha hai?

**Debug:**
```typescript
// lib/email.ts mein console.log add karo
console.log("API Key:", process.env.RESEND_API_KEY ? "Present" : "Missing");
```

### Issue 2: "Failed to send email" error

**Reason:** Resend ka free plan mein `onboarding@resend.dev` domain verified nahi hota.

**Solution:**
- Test email apne hi email pe bhejo (jo Resend account se linked hai)
- Ya custom domain verify karo

### Issue 3: `.env` file nahi padhi ja rahi

**Check:**
- `.env` file root directory mein hai? (`package.json` ke saath)
- File ka naam `.env` hai (`.env.local` nahi)?
- Server restart kiya `.env` change karne ke baad?

---

## Summary

**Aaj humne kya seekha:**

1. **Resend setup:**
   - Resend account banaya
   - API key `.env` mein add ki
   - `resend` package install kiya

2. **Email utility banaya:**
   - `lib/email.ts` banaya
   - `sendEmail()` helper function
   - Error handling add ki

3. **Test kiya:**
   - Temporary test page banaya
   - Email bheja
   - Inbox mein verify kiya

**Ab email system ready hai!** Agla step — verification token generate karna aur email mein bhejna (3.2-3.4).

---

## Cleanup

Test complete hone ke baad:
- `app/test-email/page.tsx` delete kar sakte hain
- Yeh sirf testing ke liye tha

---

## Agla Step

**3.2 — Verification flow samjho:** Ab hum verification token ka concept samjhenge — token kya hota hai, kaise kaam karta hai, aur kaise verify hota hai.